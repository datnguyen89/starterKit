@namespace SPAClient.Components

@if (Visible)
{
    <Tag Color="purple-inverse">@Text</Tag>
}
@code
{
    [Parameter]
    public bool Visible { get; set; } = true;
    [Parameter]
    public string Text { get; set; }
}
